name: Django Tests

on:
  push:
    branches: [master, main, dev, development]
  pull_request:
    branches: [master, main, dev, development]

jobs:
  test:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: github_actions
        ports:
          - 5432:5432
        # Health checks to wait until postgres has started
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pytest pytest-django
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      - name: Set up environment variables
        run: |
          echo "DJANGO_SETTINGS_MODULE=skinask_backend.settings.test" >> $GITHUB_ENV
          echo "SECRET_KEY=${{ secrets.SECRET_KEY }}" >> $GITHUB_ENV
          echo "POSTGRES_TEST_DB=github_actions" >> $GITHUB_ENV
          echo "POSTGRES_TEST_USER=postgres" >> $GITHUB_ENV
          echo "POSTGRES_TEST_PASSWORD=postgres" >> $GITHUB_ENV
          echo "POSTGRES_TEST_HOST=localhost" >> $GITHUB_ENV
          echo "POSTGRES_TEST_PORT=5432" >> $GITHUB_ENV

      - name: Run tests
        run: |
          pytest
